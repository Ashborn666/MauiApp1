<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.HomePage"
             Title="Home">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20">

        <!-- HEADER -->
        <VerticalStackLayout Grid.Row="0" Spacing="10" Margin="0,20,0,30">
            <Label Text="¡Bienvenido!"
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="Green"
                   HorizontalOptions="Center"/>

            <Label x:Name="WelcomeLabel"
                   FontSize="18"
                   TextColor="#666"
                   HorizontalOptions="Center"/>
        </VerticalStackLayout>

        <!-- CONTENT -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="20">

                <!-- EMAIL -->
                <Frame BackgroundColor="#F0F0F0" Padding="20" CornerRadius="10" HasShadow="True">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="📧 Email:"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#2D2D2D"/>
                        <Label x:Name="EmailLabel"
                               FontSize="14"
                               TextColor="#666"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- USER ID -->
                <Frame BackgroundColor="#F0F0F0" Padding="20" CornerRadius="10" HasShadow="True">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="🆔 User ID:"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#2D2D2D"/>
                        <Label x:Name="UserIdLabel"
                               FontSize="14"
                               TextColor="#666"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- USER ROLE -->
                <Frame BackgroundColor="#E3F2FD"
                       Padding="20"
                       CornerRadius="10"
                       HasShadow="True"
                       BorderColor="#2196F3">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="🔐 Rol Actual:"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#0D47A1"/>

                        <Label x:Name="RoleLabel"
                               FontSize="14"
                               TextColor="#555"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- ⭐⭐⭐ PANEL EXCLUSIVO DE ADMIN ⭐⭐⭐ -->
                <Frame x:Name="AdminPanel"
                       IsVisible="False"
                       BackgroundColor="#FFF3E0"
                       Padding="20"
                       CornerRadius="10"
                       HasShadow="True"
                       BorderColor="#FB8C00">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="⚙ Panel Administrativo"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="#E65100"
                               HorizontalOptions="Center"/>

                        <Button Text="📋 Ver lista de usuarios"
                                BackgroundColor="#FB8C00"
                                TextColor="White"
                                CornerRadius="10"
                                Clicked="OnOpenUsersList"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- SESSION INFO -->
                <Frame BackgroundColor="#E8F5E9"
                       Padding="20"
                       CornerRadius="10"
                       HasShadow="True"
                       BorderColor="Green">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="✅ Sesión activa"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="Green"
                               HorizontalOptions="Center"/>
                        <Label Text="Has iniciado sesión correctamente"
                               FontSize="12"
                               TextColor="#666"
                               HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>

        <!-- LOGOUT BUTTON -->
        <Button Grid.Row="2"
                x:Name="LogoutButton"
                Text="Cerrar Sesión"
                BackgroundColor="Green"
                TextColor="White"
                HeightRequest="50"
                CornerRadius="10"
                Margin="0,20,0,0"
                Clicked="OnLogoutClicked"/>

    </Grid>
</ContentPage>
